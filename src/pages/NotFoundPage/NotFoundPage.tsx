import { NotFound } from "./ui/NotFound";

const NotFoundPage = () => {
    return (
        <main style={{width: '100%'}}>
            <NotFound />
        </main>
    );
};

export default NotFoundPage;