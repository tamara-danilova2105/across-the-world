import { Stack } from "@/shared/ui/Stack";
import styles from './Slider.module.scss';

export const Slider = () => {
    return (
        <Stack className={styles.slider}>
            {/* TODO */}
            <div style={{width: '450px', height: '320px', background: 'grey'}} /> 
        </Stack>
    )
}